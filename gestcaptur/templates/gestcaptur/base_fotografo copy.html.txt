<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <!-- adicionado em relação ao Ubuntu -->
    <meta name="csrf-token" content="{{ csrf_token }}">
    <!-- fim adicionado em relação ao Ubuntu -->

    <title>{% block title %}Photoapp{% endblock %}</title>
    {% load static %}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
    <link rel="stylesheet" href="{% static 'gestcaptur/css/custom_mobile.css' %}"> {# Seu CSS customizado #}

    <style>
    /* 📸 ESTILOS PARA PROPORÇÃO 3:4 FIXA */
    .camera-container {
        position: relative;
        width: 100%;
        max-width: 300px;
        margin: 0 auto;
        background: #000;
        border-radius: 15px;
        overflow: hidden;
        aspect-ratio: 3/4; /* Proporção fixa 3:4 */
        box-shadow: 0 4px 20px rgba(0,0,0,0.3);
    }

    #video {
        width: 100%;
        height: 100%;
        object-fit: cover;
        object-position: center center;
        display: block;
    }

    .camera-overlay {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        pointer-events: none;
        z-index: 1;
    }

    .camera-frame-34 {
        position: absolute;
        top: 10px;
        left: 10px;
        right: 10px;
        bottom: 10px;
        border: 2px solid rgba(255, 255, 255, 0.8);
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .frame-guide {
        text-align: center;
        background: rgba(0, 0, 0, 0.6);
        padding: 8px 12px;
        border-radius: 20px;
        margin-top: -100px;
    }

    .guide-text {
        color: white;
        font-size: 12px;
        font-weight: 500;
    }

    /* 📷 PREVIEW DA FOTO CAPTURADA */
    .foto-preview {
        display: flex;
        justify-content: center;
        align-items: center;
        width: 100%;
    }

    .preview-container {
        position: relative;
        max-width: 300px;
        background: #000;
        border-radius: 15px;
        overflow: hidden;
        box-shadow: 0 4px 20px rgba(0,0,0,0.3);
    }

    .preview-container img {
        width: 100%;
        height: auto;
        display: block;
        aspect-ratio: 3/4; /* Manter proporção 3:4 no preview */
        object-fit: cover;
    }

    .preview-info {
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
        background: linear-gradient(transparent, rgba(0,0,0,0.8));
        color: white;
        padding: 15px 10px 10px;
        text-align: center;
    }

    .preview-info small {
        font-size: 11px;
        opacity: 0.9;
    }

    /* 📱 RESPONSIVO MOBILE */
    @media (max-width: 768px) {
        .modal-dialog {
            margin: 5px;
            max-width: calc(100% - 10px);
            height: calc(100vh - 10px);
        }
        
        .modal-content {
            height: 100%;
            display: flex;
            flex-direction: column;
        }
        
        .modal-body {
            flex: 1;
            padding: 15px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }
        
        .camera-container {
            max-width: 280px;
        }
        
        .preview-container {
            max-width: 280px;
        }
    }

    /* 🎯 BOTÕES DO MODAL */
    #camera-modal-footer {
        display: flex;
        justify-content: space-between;
        gap: 15px;
        padding: 20px;
        flex-shrink: 0;
        background: #f8f9fa;
    }

    #camera-modal-footer .btn {
        flex: 1;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 8px;
        min-height: 50px;
        font-weight: 600;
        border-radius: 10px;
    }

    .btn:disabled {
        opacity: 0.6;
        cursor: not-allowed;
    }

    /* 🎨 HEADER DO MODAL */
    .modal-header {
        background: linear-gradient(135deg, #007bff, #0056b3);
        color: white;
        border-bottom: none;
        border-radius: 0;
    }

    .modal-header .btn-close {
        filter: invert(1);
    }

    #aluno-info {
        background: linear-gradient(135deg, rgba(0, 123, 255, 0.1), rgba(0, 123, 255, 0.05));
        padding: 12px;
        border-radius: 10px;
        margin-bottom: 20px;
        border-left: 4px solid #007bff;
        font-weight: 600;
        text-align: center;
    }

    /* 🔄 ANIMAÇÕES */
    .camera-container, .preview-container {
        transition: all 0.3s ease;
    }

    .camera-frame-34 {
        animation: pulse 2s infinite;
    }

    @keyframes pulse {
        0%, 100% { opacity: 0.8; }
        50% { opacity: 1; }
    }

    /* 📐 GARANTIR PROPORÇÃO EM TODOS OS TAMANHOS */
    .foto-capturada img,
    .preview-container img {
        aspect-ratio: 3/4 !important;
        object-fit: cover !important;
    }
    </style>
    


    {% block extra_head %}{% endblock %} {# Para CSS ou metatags específicos da página filho #}
</head>
<body>



    {% if messages %}
        <script>
            setTimeout(function() {
            document.querySelectorAll('.alert, .django-message').forEach(function(el) {
                el.style.transition = "opacity 0.5s";
                el.style.opacity = 0;
                setTimeout(function(){ el.remove(); }, 500);
            });
            }, 5000);
        </script>
    {% endif %}    


    {# Navbar padrão do Bootstrap 5 para o collapse #}
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid"> {# container-fluid para ocupar toda a largura #}
            <a class="navbar-brand" href="">Photoapp</a> {# <-- Verifique se 'home' é o nome da URL para a página inicial #}
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto"> {# ms-auto alinha à direita #}
                    <li class="nav-item">
                        {# Exibe o nome do usuário logado. Assume que request.user está disponível #}
                        {% if request.user.is_authenticated %}
                            <span class="nav-link text-white">Olá, {{ request.user.first_name|default:request.user.username }}! ({{ request.user.get_role_display }})</span>
                        {% else %}
                             {# Opcional: Algo para usuários não autenticados, embora esta base seja para fotógrafo #}
                        {% endif %}
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{% url 'logout' %}">Sair</a> {# <-- Verifique se 'logout' é o nome da URL para sair #}
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div id="main-content"> {# Wrapper para o conteúdo principal, útil para aplicar estilos de layout se necessário #}
        {% block content %}
        {# O conteúdo específico de cada página vai aqui #}
        {% endblock %}
    </div>

    {# Bootstrap JS (Bundle inclui Popper) - COLOCADO ANTES DO SEU SCRIPT PERSONALIZADO #}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    {# SEU BLOCO DE SCRIPTS PERSONALIZADOS (renderizado a partir do {% block extra_js %}) #}
    {% block extra_js %}{% endblock %}

</body>
</html>